package com.civictwin.enums;

/**
 * Java Concept: Enum Lifecycle States.
 * Tracks incident resolution status from reporting through crew dispatch to resolution.
 */
public enum Status {
    REPORTED,
    CLASSIFIED,
    DISPATCHED,
    IN_PROGRESS,
    RESOLVED
}
