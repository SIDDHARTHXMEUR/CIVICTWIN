package com.civictwin.enums;

/**
 * Java Concept: Command Pattern Enums.
 * Actions that can be dispatched via the Decision Rail Command interface.
 */
public enum ActionType {
    DISPATCH,
    ESCALATE,
    RESOLVE,
    VERIFY
}
