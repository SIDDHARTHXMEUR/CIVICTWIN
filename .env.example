# CivicTwin - Environment Variables Template
# Note: No external API keys or environment variables are required to run this project locally.
# All spatial digital twin maps, sensor telemetry streams, and incident AI models run 
# fully self-contained out of the box.

# Optional: Set local port if overriding default Vite dev server port (Default: 5173)
# VITE_PORT=5173
